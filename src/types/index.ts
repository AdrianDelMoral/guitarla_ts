export type Guitar = {
    id: number
    image: string
    name: string
    description: string
    price: number
}

export type CartItem = {
    id: number
    image: string
    name: string
    description: string
    price: number
    quantity: number
}

